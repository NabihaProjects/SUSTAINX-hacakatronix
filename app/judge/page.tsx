import JudgeModeClient from './JudgeModeClient';

export const metadata = {
  title: 'Judge Mode — SOIL IQ Hackathon Defense',
  description: 'Clean, map-centric, high-contrast hackathon presentation console for SOIL IQ.',
};

export default function JudgePage() {
  return <JudgeModeClient />;
}
